//subtraction.c -- compile with calc_driver.c
//function definition for subtract()


//takes two pointer-to-int as arguments and returns sum of dereferenced values
int subtract(int * x, int * y)
{
  return (*x - *y);
}